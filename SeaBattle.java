
public class SeaBattle {
    // Get yourself all the instance variables you need.
    // Constructor

    int playerCount;

    public SeaBattle(int nPlayers) {
      // Initialize players

      playerCount = nPlayers;
      
      // For each player: put five ships randomly on his
      //   field without overlap
  }



  private void play() {
    // Preparations
    try {
  // As long as the game isn’t finished:
      // get the next input line and handle it.
    }
    catch (Exception e) {
      // Something went terribly wrong. Inform the user.
  } 
}

}